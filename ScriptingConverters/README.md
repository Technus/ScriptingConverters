# ScriptingConverters

## Script Escapes

* ``` `_  -> Spacebar ```
* ``` `]  -> } ```
* ``` `[  -> { ```
* ``` `)  -> > ```
* ``` `(  -> < ```
* ``` `#  -> = ```
* ``` `@  -> & ```
* ``` `*  -> ' ```
* ``` `~  -> ` ```
* ``` ``  -> " ```
* ``` `^  -> " ```
* ``` `.  -> , ```
* ``` `   -> , ```
